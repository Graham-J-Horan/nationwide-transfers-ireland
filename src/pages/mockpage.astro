<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Portfolio with Arc Background</title>
    <style>
      body {
        margin: 0;
        min-height: 100vh;
        font-family: sans-serif;
        position: relative;
        overflow-x: hidden;
      }

      /* Curved divider background */
      body::before {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: 
                /* Navy curved area */
          radial-gradient(
              ellipse 165% 140% at left 10%,
              rgb(34, 34, 138) 0%,
              rgb(6, 6, 80) 50%,
              transparent 50.1%
            )
            no-repeat,
          /* White background */ white;
        z-index: -1;
      }

      /* Content container */
      .content {
        min-height: 100vh;
        padding: 2rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      /* Example styling */
      h1 {
        color: navy;
        font-size: 3rem;
      }

      .navy-text {
        color: navy;
      }

      .white-text {
        color: white;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <h1 class="navy-text">My Portfolio</h1>
      <p class="white-text">This text appears over the navy section</p>
      <p class="navy-text">This text appears over the white section</p>
    </div>
  </body>
</html>
